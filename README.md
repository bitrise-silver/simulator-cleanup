A script to remove unused simulators
Usage:
  -l string
    	Remove simulators with versions lower than this (default "0.0")
  -p string
    	Remove simulators with platform name (default "iOS")
  -u string
    	Remove simulators with versions higher than this (default "99.0")